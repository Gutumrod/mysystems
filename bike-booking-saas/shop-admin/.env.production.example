# ==============================================================================
# shop-admin — Production Environment Variables
# ไฟล์นี้เป็นตัวอย่างค่า environment variables สำหรับ production
# ห้ามใส่ค่าจริงในไฟล์นี้ — ไฟล์นี้ commit ได้
# ค่าจริงให้ตั้งใน Vercel Project Settings → Environment Variables
# ==============================================================================

# URL ของ Supabase project
# ดูได้จาก: Supabase Dashboard → Project Settings → API → Project URL
NEXT_PUBLIC_SUPABASE_URL=

# Anon public key ของ Supabase (ปลอดภัยที่จะ expose ฝั่ง client)
# ดูได้จาก: Supabase Dashboard → Project Settings → API → anon (public)
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Service role key ของ Supabase (SECRET — ห้าม expose ฝั่ง client เด็ดขาด)
# ใช้สำหรับ server-side operations ที่ต้อง bypass RLS เช่น admin bulk update
# ดูได้จาก: Supabase Dashboard → Project Settings → API → service_role (secret)
# หมายเหตุ: ตัวแปรนี้ไม่มี NEXT_PUBLIC_ prefix → ปลอดภัย ไม่ถูกส่งไป browser
SUPABASE_SERVICE_ROLE_KEY=

# UUID ของร้านที่ต้องการ deploy
# ดูได้โดยรัน SQL ใน Supabase SQL Editor:
#   SELECT id, name FROM bike_booking.shops;
# ค่า dev ที่ใช้อยู่ (11111111-1111-1111-1111-111111111111) คือ test shop — ต้องเปลี่ยนก่อน deploy จริง
NEXT_PUBLIC_SHOP_ID=
